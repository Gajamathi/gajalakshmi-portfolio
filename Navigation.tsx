import { useState } from 'react';
const links = [['About','#about'],['Work','#work'],['Tools','#tools'],['Journey','#journey'],['Contact','#contact']];
const Navigation: React.FC = () => { const [open,setOpen]=useState(false); return <nav className="navigation" aria-label="Primary navigation"><a href="#hero" className="nav-mark" aria-label="Home">GM</a><div className="nav-links">{links.map(([label,href])=><a key={label} href={href}>{label}</a>)}<button type="button" className="nav-resume" title="Resume link will be added soon">Download Resume</button></div><button className="nav-toggle" type="button" aria-expanded={open} aria-controls="mobile-menu" onClick={()=>setOpen(!open)}>Menu</button>{open&&<div id="mobile-menu" className="mobile-menu">{links.map(([label,href])=><a key={label} href={href} onClick={()=>setOpen(false)}>{label}</a>)}<button type="button" className="nav-resume" title="Resume link will be added soon">Download Resume</button></div>}</nav>; };
export default Navigation;
